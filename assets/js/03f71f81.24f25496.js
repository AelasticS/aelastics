"use strict";(self.webpackChunk_synthesis_docs=self.webpackChunk_synthesis_docs||[]).push([[4689],{15410(e,n,t){t.r(n),t.d(n,{assets:()=>l,contentTitle:()=>i,default:()=>d,frontMatter:()=>r,metadata:()=>s,toc:()=>c});const s=JSON.parse('{"id":"developer-guide/tutorial/model-to-text/part-2-structured/multiple-documents","title":"2. Multiple documents","description":"Part 1 emitted one `` and got one file. The rule behind that is","source":"@site/docs/developer-guide/tutorial/model-to-text/part-2-structured/2-multiple-documents.md","sourceDirName":"developer-guide/tutorial/model-to-text/part-2-structured","slug":"/developer-guide/tutorial/model-to-text/part-2-structured/multiple-documents","permalink":"/aelastics/developer-guide/tutorial/model-to-text/part-2-structured/multiple-documents","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2},"sidebar":"developerGuide","previous":{"title":"1. Sections and composition","permalink":"/aelastics/developer-guide/tutorial/model-to-text/part-2-structured/sections"},"next":{"title":"3. Run and compare","permalink":"/aelastics/developer-guide/tutorial/model-to-text/part-2-structured/run-and-compare"}}');var a=t(97282),o=t(62745);const r={sidebar_position:2},i="2. Multiple documents",l={},c=[{value:"Why the constraints move to their own file",id:"why-the-constraints-move-to-their-own-file",level:2},{value:"The second document",id:"the-second-document",level:2},{value:"The statements that fill it",id:"the-statements-that-fill-it",level:2},{value:"The column rule loses a branch",id:"the-column-rule-loses-a-branch",level:2},{value:"The complete template",id:"the-complete-template",level:2},{value:"Two things worth knowing about documents",id:"two-things-worth-knowing-about-documents",level:2},{value:"Next",id:"next",level:2}];function h(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",header:"header",p:"p",pre:"pre",strong:"strong",...(0,o.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(n.header,{children:(0,a.jsx)(n.h1,{id:"2-multiple-documents",children:"2. Multiple documents"})}),"\n",(0,a.jsxs)(n.p,{children:["Part 1 emitted one ",(0,a.jsx)(n.code,{children:"<Doc>"})," and got one file. The rule behind that is\nthe whole of file layout in Synthesis: ",(0,a.jsx)(n.strong,{children:"every document in the result\nmodel becomes one file."})," So a second file is a second ",(0,a.jsx)(n.code,{children:"<Doc>"}),", and\nthere is nothing else to learn about it."]}),"\n",(0,a.jsx)(n.p,{children:"What is worth the page is why this generator wants a second file, and\nwhat the rules look like once one walk over the schema feeds one\ndocument and another walk feeds the other."}),"\n",(0,a.jsx)(n.h2,{id:"why-the-constraints-move-to-their-own-file",children:"Why the constraints move to their own file"}),"\n",(0,a.jsx)(n.p,{children:"Part 1 wrote each foreign key as an inline clause:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"CREATE TABLE Book (\n  bookId INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,\n  bookTitle VARCHAR(255),\n  AuthorId_FK INTEGER REFERENCES Author(authorId)\n);\n"})}),"\n",(0,a.jsxs)(n.p,{children:["That statement can only be applied to a database in which ",(0,a.jsx)(n.code,{children:"Author"}),"\nalready exists. For the Library schema an ordering exists that\nsatisfies every clause, so the file works. In general no such ordering\nexists, because references can be circular: two tables can reference\neach other, and a table can reference itself. There is no first table\nto create."]}),"\n",(0,a.jsx)(n.p,{children:"Production DDL solves this by separating the two concerns. Every table\nis created without its foreign keys, and the foreign keys are added\nafterwards, when every table exists:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"CREATE TABLE Book (\n  bookId INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,\n  bookTitle VARCHAR(255),\n  AuthorId_FK INTEGER\n);\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"ALTER TABLE Book ADD CONSTRAINT fk_Book_AuthorId_FK\n  FOREIGN KEY (AuthorId_FK) REFERENCES Author(authorId);\n"})}),"\n",(0,a.jsx)(n.p,{children:"Two kinds of statement, applied in order, with the second depending on\nall of the first. That is a property of the artifact, not a preference\nabout formatting, and the natural way to express it is two files."}),"\n",(0,a.jsx)(n.h2,{id:"the-second-document",children:"The second document"}),"\n",(0,a.jsx)(n.p,{children:"The constraints file is built the same way as the tables file, as a\nvalue:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",children:"    const constraintsFile = (\n      <Doc name={`${s.name}-constraints.sql`}>\n        <P>{`-- Foreign key constraints of ${s.name}, generated by Synthesis`}</P>\n        <P>{`-- Apply after ${s.name}.sql: references may be circular, so`}</P>\n        <P>{'-- every table has to exist before any of them is constrained.'}</P>\n        <P>{' '}</P>\n        {alterStatements}\n      </Doc>\n    )\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Its header says out loud what the reader of the generated file needs to\nknow, which is that this file is applied second and why. Then it embeds\n",(0,a.jsx)(n.code,{children:"alterStatements"}),", the array built above it. Reading this expression\ntells you the whole content of the file, which is the property the\nprevious page was after."]}),"\n",(0,a.jsx)(n.p,{children:"Note that the header lines are paragraphs directly in the document. A\ndocument holds document elements, and a paragraph is one, so wrapping\ntwo comment lines in a section would add a grouping nobody needs."}),"\n",(0,a.jsx)(n.h2,{id:"the-statements-that-fill-it",children:"The statements that fill it"}),"\n",(0,a.jsxs)(n.p,{children:["An ",(0,a.jsx)(n.code,{children:"ALTER TABLE"})," statement needs four names, and the model holds them in\nthree different places: the table a column belongs to, the column\nitself, and, through the ",(0,a.jsx)(n.code,{children:"references"})," link, the column it points at plus\nthe table that owns it. A small local type collects them, so the code\nthat formats the statement does no navigation at all:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",children:"/** One foreign key: the column that points, and the column it points at. */\ntype ForeignKey = {\n  table: rmT.ITable\n  column: rmT.IColumn\n  targetTable: rmT.ITable\n  targetColumn: rmT.IColumn\n}\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",children:"  // Every referencing column of the schema, paired with the table and\n  // column it points at. A column is a foreign key exactly when it has a\n  // reference, so this walk is the complete list.\n  private foreignKeysOf(s: rmT.IRelSchema): ForeignKey[] {\n    const tables = this.tablesOf(s)\n    const keys: ForeignKey[] = []\n    for (const table of tables) {\n      for (const column of table.columns ?? []) {\n        if (column.references) {\n          keys.push({\n            table,\n            column,\n            targetTable: this.tableOwning(s, column.references),\n            targetColumn: column.references\n          })\n        }\n      }\n    }\n    return keys\n  }\n"})}),"\n",(0,a.jsxs)(n.p,{children:["This is Part 1's ",(0,a.jsx)(n.code,{children:"if (c.references)"})," test, moved out of the line\nformatter and into a walk of its own. ",(0,a.jsx)(n.code,{children:"tableOwning"})," is Part 1's helper,\nunchanged: a column has no back link to its table, so the table a\nreference names is found by searching the schema."]}),"\n",(0,a.jsx)(n.p,{children:"Separating the walk from the formatting is what makes the statements\nthemselves short:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",children:"    const alterStatements = foreignKeys.map((fk) => {\n      const constraint = `fk_${fk.table.name}_${fk.column.name}`\n      return (\n        <Sec name={`constraint_${fk.table.name}_${fk.column.name}`}>\n          <P>{`ALTER TABLE ${fk.table.name} ADD CONSTRAINT ${constraint}`}</P>\n          <P>{`  FOREIGN KEY (${fk.column.name}) REFERENCES ${fk.targetTable.name}(${fk.targetColumn.name});`}</P>\n          <P>{' '}</P>\n        </Sec>\n      )\n    })\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Put this beside ",(0,a.jsx)(n.code,{children:"createStatements"})," from the previous page and they are\nthe same shape: read something out of the model, produce one section per\nitem, name each section after what it came from. Neither array mentions\na document. Which file they end up in is decided once, where the\ndocuments are built."]}),"\n",(0,a.jsxs)(n.p,{children:["The constraint name is derived rather than invented:\n",(0,a.jsx)(n.code,{children:"fk_Book_AuthorId_FK"})," is the table and the column. Constraint names have\nto be unique in a schema and are what a later migration uses to drop a\nconstraint, so deriving them from the model is what makes regenerating\nthe file produce the same names again."]}),"\n",(0,a.jsx)(n.h2,{id:"the-column-rule-loses-a-branch",children:"The column rule loses a branch"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",children:"  private columnLine(c: rmT.IColumn): string {\n    const parts: string[] = [c.name, c.type]\n    if (c.isKey) {\n      parts.push('PRIMARY KEY')\n    }\n    if (c.isAutoincrement) {\n      parts.push('AUTOINCREMENT')\n    }\n    if (!c.isNullable) {\n      parts.push('NOT NULL')\n    }\n    return parts.join(' ')\n  }\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Compared with Part 1, the ",(0,a.jsx)(n.code,{children:"if (c.references)"})," branch is gone, and the\nschema parameter with it, because nothing in this method needs to look\nanything up any more. The same fact about the model, a column having a\n",(0,a.jsx)(n.code,{children:"references"})," link, is still what produces a foreign key. It is read in\none place instead of two, and it produces a statement in the other file\ninstead of a clause in this one."]}),"\n",(0,a.jsx)(n.h2,{id:"the-complete-template",children:"The complete template"}),"\n",(0,a.jsxs)(n.p,{children:["The previous page showed the first document and this page showed the\nsecond, both as fragments. Here is the finished ",(0,a.jsx)(n.code,{children:"template()"}),", embedded\nfrom the companion package itself rather than copied, so it cannot\ndrift from the code that actually runs:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-tsx",metastring:"file=<rootDir>/examples/tutorial/sql-tutorial-structured/src/transformation.tsx#L41-L99",children:"  template(s: rmT.IRelSchema) {\n    // Read the model once, up front: the tables to create, and every\n    // foreign key that has to be added after they all exist.\n    const tables = this.tablesOf(s)\n    const foreignKeys = this.foreignKeysOf(s)\n\n    // One section per statement. Each is an ordinary value, so the\n    // documents below can be read as the files they produce.\n    const createStatements = tables.map((tab) => {\n      const cols = tab.columns ?? []\n      return (\n        <Sec name={`table_${tab.name}`}>\n          <P>{`CREATE TABLE ${tab.name} (`}</P>\n          {cols.map((c, i) => (\n            <P>{`  ${this.columnLine(c)}${i < cols.length - 1 ? ',' : ''}`}</P>\n          ))}\n          <P>{');'}</P>\n          <P>{' '}</P>\n        </Sec>\n      )\n    })\n\n    const alterStatements = foreignKeys.map((fk) => {\n      const constraint = `fk_${fk.table.name}_${fk.column.name}`\n      return (\n        <Sec name={`constraint_${fk.table.name}_${fk.column.name}`}>\n          <P>{`ALTER TABLE ${fk.table.name} ADD CONSTRAINT ${constraint}`}</P>\n          <P>{`  FOREIGN KEY (${fk.column.name}) REFERENCES ${fk.targetTable.name}(${fk.targetColumn.name});`}</P>\n          <P>{' '}</P>\n        </Sec>\n      )\n    })\n\n    // Two documents, so two files. Each states its whole content.\n    const tablesFile = (\n      <Doc name={`${s.name}.sql`}>\n        <P>{`-- Tables of ${s.name}, generated by Synthesis`}</P>\n        <P>{' '}</P>\n        {createStatements}\n      </Doc>\n    )\n\n    const constraintsFile = (\n      <Doc name={`${s.name}-constraints.sql`}>\n        <P>{`-- Foreign key constraints of ${s.name}, generated by Synthesis`}</P>\n        <P>{`-- Apply after ${s.name}.sql: references may be circular, so`}</P>\n        <P>{'-- every table has to exist before any of them is constrained.'}</P>\n        <P>{' '}</P>\n        {alterStatements}\n      </Doc>\n    )\n\n    return (\n      <M2T name={`${s.name}_sql`}>\n        {tablesFile}\n        {constraintsFile}\n      </M2T>\n    )\n  }\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Read it top to bottom and it is four steps with nothing hidden between\nthem. The model is read twice, into ",(0,a.jsx)(n.code,{children:"tables"})," and ",(0,a.jsx)(n.code,{children:"foreignKeys"}),". Each\nread becomes an array of sections. Each array is embedded in the\ndocument it belongs to. The two documents are returned as the model."]}),"\n",(0,a.jsx)(n.p,{children:'The last expression is the one worth pausing on, because it is the\nanswer to "what does this transformation produce": a model of two files.\nEverything above it is how those two files are filled, and every step is\na value you can name, log, or reuse.'}),"\n",(0,a.jsx)(n.h2,{id:"two-things-worth-knowing-about-documents",children:"Two things worth knowing about documents"}),"\n",(0,a.jsxs)(n.p,{children:[(0,a.jsx)(n.strong,{children:"This package writes a flat set of files, but folders are available."}),"\nEvery document here is written directly under ",(0,a.jsx)(n.code,{children:"--out-dir"}),", and the\ngrouping this package needs lives in the file names, as with\n",(0,a.jsx)(n.code,{children:"Library_rel-constraints.sql"}),". For real folders, wrap documents in a\ndirectory: ",(0,a.jsx)(n.code,{children:'<Dir name="ddl">'})," around the two ",(0,a.jsx)(n.code,{children:"<Doc>"})," elements writes\nthem to ",(0,a.jsx)(n.code,{children:"ddl/"})," under the output directory. A directory holds documents\nand other directories, so only the documents move inside it."]}),"\n",(0,a.jsxs)(n.p,{children:[(0,a.jsx)(n.strong,{children:"Two documents cannot share a name."})," Documents are model elements,\nand in the text metamodel, which declares no name scopes, element\nnames are unique across a whole model rather than per container. Give both documents the same name and the run fails while\nthe model is still being built, before any file is written:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"Error: Transformation 'sql-tutorial-structured' failed: newModelElement:\nDuplicate name \"//www.aelastics.org/Library_rel_sql/Library_rel.sql\"\n"})}),"\n",(0,a.jsx)(n.p,{children:"The name in the message is the qualified element name, which is the\nmodel's name followed by the element's. In practice the rule costs\nnothing, since two documents with the same name would be two files with\nthe same path, which was never going to work. It is worth recognizing\nthe message, because it names the metamodel's rule rather than the file\nsystem's, and it is the same rule that makes the section names on the\nprevious page carry their table name."}),"\n",(0,a.jsx)(n.h2,{id:"next",children:"Next"}),"\n",(0,a.jsxs)(n.p,{children:["Both documents exist and both are filled. ",(0,a.jsx)(n.a,{href:"/aelastics/developer-guide/tutorial/model-to-text/part-2-structured/run-and-compare",children:"Run and\ncompare"})," puts the package to work."]})]})}function d(e={}){const{wrapper:n}={...(0,o.R)(),...e.components};return n?(0,a.jsx)(n,{...e,children:(0,a.jsx)(h,{...e})}):h(e)}},62745(e,n,t){t.d(n,{R:()=>r,x:()=>i});var s=t(30538);const a={},o=s.createContext(a);function r(e){const n=s.useContext(o);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:r(e.components),s.createElement(o.Provider,{value:n},e.children)}}}]);