
// TODO: Define query decorators for transformations that handle query operations in a similar way to the variability decorators. 
// Difference to trace (rule, mapping, E2E) decorators is that output is not a element of target model, while input must be an element of source model.
// Results of queries are stored in the trace, similar to rules (mappings, E2E).

// All other methods and functions in transformation are threated as helpers, and are not stored in the trace.